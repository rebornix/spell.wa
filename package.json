{
    "name": "spell-wa",
    "displayName": "spell.wa",
    "description": "",
    "version": "0.0.1",
    "publisher": "rebornix",
    "engines": {
        "vscode": "^1.28.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "*"
    ],
    "main": "./out/extension",
    "contributes": {
        "commands": [
            {
                "command": "extension.spell",
                "title": "Hello World"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "npm run compile && node ./node_modules/vscode/bin/test"
    },
    "dependencies": {
        "hunspell-asm": "2.0.0-beta.2",
        "node-monarch": "github:rebornix/node-monarch#master",
        "unixify": "^1.0.0"
    },
    "devDependencies": {
        "@types/mocha": "^2.2.48",
        "@types/node": "^8.10.25",
        "typescript": "^2.6.1",
        "vscode": "^1.1.21"
    }
}
